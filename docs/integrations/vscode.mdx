---
title: VS Code
---

VS Code includes built-in AI chat through GitHub Copilot Chat. Lychee models can be used directly in the Copilot Chat model picker.


![VS Code with Lychee](/images/vscode.png)


## Prerequisites

- Lychee v0.18.3+
- [VS Code 1.113+](https://code.visualstudio.com/download)
- [GitHub Copilot Chat extension 0.41.0+](https://marketplace.visualstudio.com/items?itemName=GitHub.copilot-chat)

<Note> VS Code requires you to be logged in to use its model selector, even for custom models. This doesn't require a paid GitHub Copilot account; GitHub Copilot Free will enable model selection for custom models.</Note>

## Quick setup

```shell
lychee launch vscode
```

Recommended models will be shown after running the command. See the latest models at [lychee.ai](https://lychee.ai/search?c=tools).

Make sure **Local** is selected at the bottom of the Copilot Chat panel to use your Lychee models.
<div style={{ display: "flex", justifyContent: "center" }}>
  <img
    src="/images/local.png"
    alt="Lychee Local Models"
    width="60%"
    style={{ borderRadius: "4px", marginTop: "10px", marginBottom: "10px" }}
  />
</div>


## Run directly with a model

```shell
lychee launch vscode --model qwen3.5:cloud
```
Cloud models are also available at [lychee.ai](https://lychee.ai/search?c=cloud).

## Manual setup

To configure Lychee manually without `lychee launch`:

1. Open the **Copilot Chat** side bar from the top right corner
   <div style={{ display: "flex", justifyContent: "center" }}>
     <img
       src="/images/vscode-sidebar.png"
       alt="VS Code chat Sidebar"
       width="75%"
       style={{ borderRadius: "4px" }}
     />
   </div>
2. Click the **settings gear icon** (<Icon icon="gear" />) to bring up the Language Models window
   <div style={{ display: "flex", justifyContent: "center" }}>
     <img
       src="/images/vscode-other-models.png"
       alt="VS Code model picker"
       width="75%"
       style={{ borderRadius: "4px" }}
     />
   </div>
3. Click **Add Models** and select **Lychee** to load all your Lychee models into VS Code
   <div style={{ display: "flex", justifyContent: "center" }}>
     <img
       src="/images/vscode-add-lychee.png"
       alt="VS Code model options dropdown to add lychee models"
       width="75%"
       style={{ borderRadius: "4px" }}
     />
   </div>

4. Click the **Unhide** button in the model picker to show your Lychee models  
   <div style={{ display: "flex", justifyContent: "center" }}>
     <img
       src="/images/vscode-unhide.png"
       alt="VS Code unhide models button"
       width="75%"
       style={{ borderRadius: "4px" }}
     />
   </div>
